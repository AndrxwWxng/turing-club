import React from 'react';
import Image from 'next/image';
import Link from 'next/link';

const aboutPage = () => {
    return (
        <div className="py-2 px-4 z-50 items-center justify-between">
        <span>The Turing Club is all about spreading computer science. It has been a key part of the CS Forum at Allen High School for over half a decade. </span>
        </div>
    );
};

export default aboutPage;